# ==============================================================================
# Waterfall Trial Configuration Example
# ==============================================================================
# Copy this file to .env and configure your settings
# Most defaults are fine for trial/demo purposes

# ------------------------------------------------------------------------------
# PostgreSQL Database Configuration
# ------------------------------------------------------------------------------
POSTGRES_USER=waterfall
POSTGRES_PASSWORD=waterfall_secure_password_change_me
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Database URLs (automatically constructed from above)
DATABASE_URL_AUTH=postgresql://waterfall:waterfall_secure_password_change_me@postgres:5432/waterfall_auth
DATABASE_URL_IDENTITY=postgresql://waterfall:waterfall_secure_password_change_me@postgres:5432/waterfall_identity
DATABASE_URL_GUARDIAN=postgresql://waterfall:waterfall_secure_password_change_me@postgres:5432/waterfall_guardian

# ------------------------------------------------------------------------------
# Application Security
# ------------------------------------------------------------------------------
# Leave empty to auto-generate on first run (recommended for trial)
JWT_SECRET=
INTERNAL_AUTH_TOKEN=

# ------------------------------------------------------------------------------
# Application Configuration
# ------------------------------------------------------------------------------
FLASK_ENV=production
LOG_LEVEL=info
NODE_ENV=production

# ------------------------------------------------------------------------------
# Exposed Ports
# ------------------------------------------------------------------------------
# These are the ports exposed to your host machine
HTTP_PORT=8080
HTTPS_PORT=8443

# ------------------------------------------------------------------------------
# Redis (Optional - requires profile: cache)
# ------------------------------------------------------------------------------
# Uncomment and configure if using Redis profile
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# ------------------------------------------------------------------------------
# MinIO (Optional - requires profile: storage)
# ------------------------------------------------------------------------------
# Uncomment and configure if using MinIO profile
# MINIO_ROOT_USER=minioadmin
# MINIO_ROOT_PASSWORD=minioadmin_change_me
# MINIO_HOST=minio
# MINIO_PORT=9000
# MINIO_CONSOLE_PORT=9001

# ------------------------------------------------------------------------------
# Advanced Settings (Usually no need to change)
# ------------------------------------------------------------------------------
# GUNICORN_WORKERS=2
# GUNICORN_TIMEOUT=30
# NEXT_PUBLIC_AUTH_API_URL=/api/auth
# NEXT_PUBLIC_IDENTITY_API_URL=/api/identity
# NEXT_PUBLIC_GUARDIAN_API_URL=/api/guardian
